// Feature flags. Flip these to toggle whole features in the UI without deleting
// any code or data.
//
// Reviews are fully built (submission, star display, rating rollup) but hidden
// for now. Set FEATURE_REVIEWS back to `true` to surface them again everywhere:
// clinic cards, clinic profiles, and the "leave a review" flow on past visits.
export const FEATURE_REVIEWS = false;
